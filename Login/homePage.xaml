<Window x:Class="Login.homePage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Login"
        mc:Ignorable="d"
        Title="homePage" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top" Background="#ECF0F1" Foreground="Black">
            <MenuItem Header="Operaciones">
                <MenuItem Header="Ingresos" Click="registrarIngresos"/>
                <MenuItem Header="Salidas"/>
            </MenuItem>

            <MenuItem Header="Mantenimientos">
                <MenuItem Header="Conductores"/>
                <MenuItem Header="Transportistas"/>
                <MenuItem Header="Camiones"/>
                <MenuItem Header="Productos"/>
            </MenuItem>

            <MenuItem Header="Reportes">
                <MenuItem Header="Cargas"/>
                <MenuItem Header="Ingresos"/>
                <MenuItem Header="Salidas"/>
            </MenuItem>

            <MenuItem Header="Salir"/>
        </Menu>

        <Grid Background="#ECF0F1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Bienvenido al Sistema" 
                           FontSize="24" FontWeight="Bold" 
                           Foreground="#2C3E50" HorizontalAlignment="Center"/>

                <TextBlock Text="Seleccione una opción del menú superior para continuar"
                           FontSize="14" Foreground="Gray" 
                           Margin="0,10,0,20" HorizontalAlignment="Center"/>

                <WrapPanel HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Ingresos" Width="120" Height="40" Margin="10" Click="registrarIngresos"/>
                    <Button Content="Salidas" Width="120" Height="40" Margin="10"/>
                    <Button Content="Conductores" Width="120" Height="40" Margin="10"/>
                    <Button Content="Reportes" Width="120" Height="40" Margin="10"/>
                </WrapPanel>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
